{% extends "blog_cuisineFrontBundle:FrontGeneral:navigation.html.twig" %}

{% block title %}Recette{% endblock %}
{% block main %}
    {{parent()}}
    <br><br><br>
    
        <div class="row">
            <div class="col-md-4">
                <img class="imgAppli" src="{{ asset('bundles/blogback/imgRecette/')}}{{article.chemin}} " alt="{{article.titre}}" />
            </div>
            <div class="col-md-8">
                <h2>{{article.titre}}</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                {% autoescape false %}
                    {{article.description}}
                {% endautoescape %}
            </div>
        </div>
    
    <div class="row">
        <div class="col-xs-12">
            <div class="retourPage">
                <a href="#haut">retour en haut de la page</a>
            </div>
        </div>
    </div>

{% endblock %}
